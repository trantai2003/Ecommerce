server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: ecommerce

  # ---------- DATASOURCE (PostgreSQL local) ----------
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Tai171003@}        # đổi thành mật khẩu postgres local của bạn
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: EcommerceHikariPool
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # ---------- JPA / HIBERNATE ----------
  jpa:
    open-in-view: false
    show-sql: false                         # log SQL qua logging bên dưới
    hibernate:
      ddl-auto: none                    # schema do schema.sql quản lý, Hibernate chỉ kiểm tra
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
        default_batch_fetch_size: 50        # giảm N+1 khi load quan hệ LAZY

  # Không để Spring tự chạy schema.sql/data.sql
  sql:
    init:
      mode: never

  # ---------- JSON ----------
  jackson:
    time-zone: Asia/Ho_Chi_Minh
    default-property-inclusion: non_null

  # ---------- UPLOAD ẢNH ----------
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

# ---------- JWT (cau hinh rieng cua ung dung, KHONG nam duoi spring:) ----------
app:
  jwt:
    # Key bí mật độ dài tối thiểu 256-bit (dùng mã hóa HS256)
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    # Thời gian hết hạn của token: 1 ngày (86,400,000 milliseconds)
    expiration-ms: 86400000

# ---------- LOGGING ----------
logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG                # in câu SQL
    org.hibernate.orm.jdbc.bind: TRACE      # in giá trị tham số (Hibernate 6)
    com.dev.backend: DEBUG

